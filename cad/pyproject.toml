[project]
name = "g55-public-cad"
version = "0.1.0"
description = "Agent-assisted STL review tooling for code-first parametric CAD"
requires-python = ">=3.12,<3.13"
dependencies = [
    "build123d>=0.11",
    "numpy>=2.0",
]

[tool.uv]
package = false
